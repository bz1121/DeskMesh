# DeskMesh（桌联）隐私说明

DeskMesh 是本地优先的软件。项目本身不提供云端中继、账号系统、广告、遥测或崩溃自动上传。控制网页只应监听本机回环地址，设备之间的数据只在用户配置的局域网连接上传输。

## 会处理的数据

- **设备身份与信任关系**：设备标识、名称、证书、公钥指纹、已配对设备和设置保存在本机应用数据目录。身份私钥使用 Windows DPAPI 保护。
- **剪贴板**：启用同步后，当前文本或图片会发送到已配对设备。DeskMesh 不设计为长期剪贴板历史工具。
- **文件**：只有发送方主动选择、接收方确认的文件才会传输。传输可能使用本机临时 `.part` 或暂存文件，完成、取消或失败后按相应流程处理。
- **远程桌面与音频**：启用会话后，屏幕帧、输入事件和系统回放音频会实时发送给已配对设备；DeskMesh 不主动录制这些流。
- **诊断信息**：本机界面可能显示有限数量的运行诊断。分享日志前应删除设备名、IP、文件路径、证书指纹和个人内容。

Alpha 当前可能沿用内部兼容数据目录 `%LOCALAPPDATA%\LanSwitch`。具体目录可通过启动参数覆盖；公开品牌始终为 DeskMesh（桌联）。

## 谁可以收到数据

只有本机用户主动配对并仍被信任的设备，才应通过设备接口接收功能数据。操作系统、网络设备、安全软件以及用户选择的文件保存位置仍可能按各自策略处理数据。恶意或已经失陷的已配对设备不在 DeskMesh 能够防御的范围内。

## 用户控制

你可以暂停剪贴板同步、关闭音频或远程桌面、取消文件传输、撤销设备信任并退出程序。删除应用数据会同时删除本机身份、设置和配对关系；操作前请先退出所有 DeskMesh 实例并确认不再需要这些数据。

## 数据安全

设备连接使用双向 TLS、设备证书指纹固定和会话级重放保护。任何软件都无法在已被管理员或恶意软件控制的 Windows 会话中保证数据机密性。请保持 Windows 更新、仅使用专用网络，并核对首次配对时显示的安全信息。

隐私或安全问题请按 [SECURITY.md](SECURITY.md) 私密报告。
