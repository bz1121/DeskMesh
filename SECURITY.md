# DeskMesh（桌联）安全策略

DeskMesh 能转发键鼠、剪贴板、文件、屏幕和系统音频。请把它当作远程控制软件部署，只在你信任的家庭或办公专用局域网中使用，不要把设备通信端口直接暴露到公网。

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `0.1.x` Alpha | 支持 |
| 更早的开发快照 | 不支持 |

当前版本是未签名 Alpha，尚未接受独立安全审计。实验性的远程桌面、系统音频和最高 90 FPS 模式不应视为生产级远程运维方案。

## 私密报告漏洞

请不要为潜在漏洞创建公开 Issue。使用 GitHub 的 [Private vulnerability reporting](https://github.com/bz1121/DeskMesh/security/advisories/new) 提交私密报告。如果该入口不可用，请通过维护者 [@bz1121](https://github.com/bz1121) 的 GitHub 资料页提供的私密联系方式联系。

报告中请尽量包含：

- 受影响版本、提交号和 Windows 版本；
- 攻击前提、可信边界和可重复的最小步骤；
- 对机密性、完整性、可用性的实际影响；
- 已脱敏的日志、数据包或概念验证；
- 你认为安全的修复或缓解建议。

不要提交真实配对码、私钥、原始身份文件、个人剪贴板内容或未脱敏的远程画面。我们以 7 天内确认收到、30 天内提供初步处置意见为目标，但 Alpha 阶段不承诺固定 SLA。

## 优先处理的问题

- 未经配对即可访问设备接口、远程画面、音频、剪贴板或输入通道；
- 绕过证书指纹固定、重放保护、会话 epoch 或撤销信任；
- 文件传输中的路径穿越、任意覆盖、摘要绕过或临时文件逃逸；
- localhost 控制面的跨站请求、来源校验或会话令牌绕过；
- 远程输入无法释放，导致持续控制或本机无法恢复；
- 敏感信息被日志、错误响应或发布物意外暴露。

以下情况通常不属于漏洞：需要本机管理员权限或已完全控制任一已配对设备；公开公网端口后产生的扫描流量；显示器不正确实现 DDC/CI；Windows 明确禁止控制的 UAC 安全桌面、锁屏、登录前界面和 Ctrl+Alt+Del。

## 协调披露

请在维护者确认修复或双方约定披露日期前保持细节私密。修复发布后，项目会在安全公告和变更日志中说明影响版本、缓解措施与致谢；报告者可选择匿名。
